# إعداد أداة الدبلجة (مرة وحدة بس)

## طريقة العمل

بسبب إن يوتيوب صار يحظر التحميل من سيرفرات GitHub (يطلب "تأكيد أنك مو بوت")،
صار التحميل من يوتيوب يصير **محليًا على جوالك** (ترمكس، IP طبيعي).

**الفيديو نفسه ما يطلع من جوالك أبدًا.** بعد التحميل المحلي:
1. جوالك يفصل الصوت من الفيديو (ملف صغير جدًا مقارنة بالفيديو)
2. يرفع الصوت بس كـ GitHub Release مؤقت
3. GitHub Actions يفرّغ الكلام، يترجم، ويولّد صوت عربي مطابق للتوقيت
4. جوالك ينزّل الصوت المدبلج (صغير برضو) ويدمجه محليًا مع الفيديو الأصلي

هذا يوفّر رفع/تنزيل ضخم وباندوث ووقت، لأن أثقل ملف (الفيديو) ما يغادر جوالك إطلاقًا.

## 1) أنشئ مستودع GitHub ثم ارفع الكود

راجع الخطوات اللي سويتها سابقًا (git init / commit / push أو `gh repo create`).
المستودع لازم يكون **Public** حتى تشتغل دقائق Actions مجانًا بدون قيود فوترة.

## 2) ثبّت الأدوات المطلوبة على ترمكس

```bash
pkg update -y
pkg install -y git gh python ffmpeg
pip install -U yt-dlp
```

## 3) سجّل دخول gh

```bash
gh auth login
```
اختر `GitHub.com` → `HTTPS` → `Login with a web browser`

## 4) التشغيل اليومي (أمر واحد بس)

```bash
cd ~/youtube-dub-github
bash local-tools/dub_from_phone.sh "https://youtu.be/XXXXXXX" small
```

المعامل الثاني اختياري (حجم موديل الويسبر): `tiny` / `base` / `small` (افتراضي) / `medium`

هذا السكربت يسوّي تلقائيًا:
1. يحمّل الفيديو من يوتيوب محليًا على جوالك
2. يفصل الصوت من الفيديو محليًا (ffmpeg)
3. يرفع **الصوت بس** كإصدار مؤقت (Release) على GitHub — ملف خفيف
4. يشغّل عملية الدبلجة على GitHub Actions (تفريغ + ترجمة + توليد صوت عربي)
5. ينتظر لحد ما يخلص ويطبع التقدم مباشرة
6. ينزّل **الصوت المدبلج بس** (خفيف برضو)
7. يدمج الصوت المدبلج مع الفيديو الأصلي محليًا (ffmpeg)
8. يحذف كل الملفات المؤقتة

الفيديو النهائي بيكون بمجلد `~/storage/downloads` باسم مثل `dubbed_20260831_143000.mp4`.

## ملاحظات مهمة

- كل تشغيلة على GitHub Actions مجانية وغير محدودة تقريبًا للمستودعات العامة.
- التحميل المحلي يحتاج اتصال إنترنت جيد بجوالك (يرفع فيديو كامل الجودة لـ GitHub).
- الإصدار (Release) المؤقت ينحذف تلقائيًا آخر خطوة بالـ workflow — لو صار خطأ
  بمنتصف العملية وما انحذف، احذفه يدويًا:
  ```bash
  gh release delete <اسم_الإصدار> --yes
  ```
- الترجمة تستخدم خدمة قوقل المجانية وممكن تتأخر أحيانًا — الكود فيه إعادة محاولة تلقائية.
- احترم حقوق الفيديوهات اللي تدبلجها — هذي الأداة لاستخدامك الشخصي.
